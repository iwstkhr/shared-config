# Skip files ignored by Git, such as .venv and node_modules
gitignore: true

config:
  # Disable MD013 (line length): Japanese text often exceeds 80 columns when wrapped.
  MD013: false
  # Disable MD029 (ordered list item prefix): we number lists as 1. 2. 3. throughout.
  MD029: false
